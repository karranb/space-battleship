import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.spacebattleship.app',
  appName: 'Space BattleShip',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
